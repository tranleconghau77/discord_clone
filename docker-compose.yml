version: "3.1"

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MY_DATABASE: discord
      MYSQL_ROOT_PASSWORD: my-secret-pw
    volumes:
      - mysql:/var/lib/mysql

volumes:
  mysql:
